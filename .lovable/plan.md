

## Rename "WSF Centres" to "Home Cell Report" in Sidebar

### Change

In `src/components/AppLayout.jsx`, update the nav item entry:

- Line ~48: Change `{ name: "WSF Centres", icon: Globe, path: "/wsf", access: "wsf" }` to `{ name: "Home Cell Report", icon: Globe, path: "/wsf", access: "wsf" }`

### Files changed
- **Edit**: `src/components/AppLayout.jsx` — one label change

