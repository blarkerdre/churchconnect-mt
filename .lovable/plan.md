# OC1 evidence images from GitHub

## Step 1 — You connect GitHub (needed before I can capture anything)

In the editor: **+** menu → **GitHub** → **Connect project** → authorise → choose your account → **Create Repository**. The full codebase pushes automatically. Then tell me the repository URL.

Until that exists there is no repo page to photograph, so nothing else in this plan can run.

## Step 2 — Capture the GitHub evidence

For each of the four OC1 innovations I capture repository screenshots that show the code as it lives in version control — file path, repository name and line numbers all visible, which is what makes a GitHub screenshot stronger evidence than a pasted snippet.

| Innovation | What the image shows |
|---|---|
| Database-enforced multi-tenancy | The migration defining the null-safe `user_has_tenant_access` oracle, plus the repository file tree showing the volume of migrations |
| Child safeguarding custody flow | The check-in page source with the guardian/child PIN state machine |
| IP-free analytics | The tracking function showing geography resolved without storing an address |
| Self-healing recovery | The reload-guard module with the cooldown logic |

Plus one overview image: the repository landing page (owner, name, language breakdown, commit count) establishing authorship and scale.

If the repository is private I will sign in through your session; if that is not possible I will ask you to paste the five screenshots and I will place them.

## Step 3 — Rebuild the document

A new version, **Global-Talent-Technical-Evidence_v2.docx**, identical to the current one except that each OC1 sub-section gains its GitHub figure with a caption naming the repository, file path and line range. The original file stays untouched. The appendix traceability index gains a column pointing each OC1 claim at its repository path.

## Notes

- No application code changes.
- Any personal data visible in a screenshot is blurred before it goes in.
- I review every page as an image before delivering.
